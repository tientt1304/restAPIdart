class NoteForListening {
  String? noteID;
  String? noteTitle;
  DateTime? createDateTime;
  DateTime? lastestEditDateTime;
  NoteForListening(
      {this.noteID,
      this.noteTitle,
      this.createDateTime,
      this.lastestEditDateTime});
}
